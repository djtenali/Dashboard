name: Workflow-2
on:
  workflow_run:
    workflows:
      - Workflow-1
    types: 
    - completed
jobs:
    job1:
        if: ${{ github.event.workflow_run.conclusion == 'success' }}
        runs-on: ubuntu-latest
        steps:
        - name: Printing 
          run: echo "This workflow is triggered beacause Workflow-1 is completed "